# UserSessionExtensions

Provides extension methods for managing user session state within the Telegram bot framework. These methods allow inspection and manipulation of session lifetime, context data, and user-specific session tracking without direct access to the underlying session storage mechanism.

## API

### `IsIdle`
